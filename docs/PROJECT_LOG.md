# Project Log — Rural Electrification Planning App

This file tracks scope decisions, open questions, and progress across sessions. Read this first when resuming work.

## Scope

- **Phase 1 (current): Solar PV only.**
- Wind, diesel generator, hydropower, biomass are explicitly deferred to later phases. Do not build UI or calculation branches for these yet — the app architecture should stay extensible (e.g. a "generation source" selector that currently only offers Solar PV), but no wind/diesel/hydro/biomass logic should be implemented now.
- Web framework: **Streamlit** (user's choice).
- Reference source: `Base-Calculations-00.xlsx` (renamed by the user from `Base-Calculations.xlsx`), in the user's `Rural Electrification` folder on their device, and staged into this project for read-only reference. Original workbook sheets referenced as "Annex-I" through "Annex-VII" in the user's own description map to the actual tab names `Annex-I`, `Annex-II`, `Annex-III`, `Annex-IIIa`, `Annex-IIIb`, `Annex-DP (for Pivot)`, `Annex-IV`, `Annex-V`, `Annex-Va`, `Annex-Vb`, `Annex-VI`, `Annex-VII`. Full methodology extraction: `docs/Workbook-Methodology-Reference.md`.
- Site in the reference workbook: "Shamspir" (coastal Pakistan-like site), 1,480 households across 3 socioeconomic categories, currency PKR (converted to EUR at a fixed 330 PKR/EUR in the source file).

## Planned build order (per user's description, one notebook per core step, executed only when the user says go)

1. **Connected Load Estimation** (≈ Annex-I) — households × appliance list per category + editable Misc. loads, tabular input + Excel upload option.
2. **Hourly Load Profile of the Year** (≈ Annex-II + Annex-III) — day-type patterns (season/weekend/holiday/festival) → full 8,760-hour annual profile; option to upload a full hourly profile directly instead.
3. **Load Profile Visualization** — weekly/monthly/yearly graphs, switchable units (kWh/MWh/TWh).
4. **Solar Resource & PV/Battery Sizing** (≈ Annex-IV) — download annual irradiation (PVGIS/NASA POWER), Ppeak formula (Q, Iqc), Egen, Delta E, battery SOC simulation, battery sizing, zero-electricity-hours, user-editable battery size with live curve.
5. **Cost / BOQ / LCOE** (≈ Annex-VI) — unit costs with currency toggle, lump-sum override, inflation-adjusted O&M present value, LCOE.
6. **Summary Table** — annual demand, connected load, system type/size, battery size, total cost, O&M, LCOE (default 30 yrs, user-adjustable), ROI, other key figures.

## Decisions (resolved by user on 2026-09-02)

1. **Calendar mapping bug → FIX IT.** Step 2 build will use the seasonally-correct Annex-II blocks ("Peak Monsoon" and "Late Summer" weekday profiles) for the mid-Aug–mid-Oct period, not the source file's accidental April-block reuse.
2. **Battery sizing derating factor → user-editable, default 0.85.** The source workbook's undocumented `×0.90` factor (applied after the 0.80 Depth-of-Discharge division) becomes a single user-editable "battery quality/derating factor" input, defaulting to **0.85**. Depth-of-Discharge (0.80) stays as a separate, also user-editable input. Exact formula to be finalized and shown to the user at Step 4 (e.g. `battery_kWh = round_to_1000(max_deficit_kWh / DoD * quality_factor)`), confirming the interpretation before locking it in.
3. **LCOE denominator → total PV generation**, not total demand. `LCOE = Total Cost (Capex + PV of Opex) / Total PV Generation`, matching the user's own correction. Implement at Step 5.
4. **Development workflow → cloud-execute, then sync, plus a matching local venv.** The user wants to keep VS Code open on their own machine, watching each step land in `D:\Claude Co Work\Code\Rural Electrification` as it's built. Since this session has no shell access to the user's computer (file read/write only, no command execution there), the actual build-and-test loop stays in this cloud sandbox venv; each finished notebook is executed here (so outputs/charts/tables are baked into the `.ipynb` JSON) and then synced to the user's folder, viewable in VS Code immediately, no execution required on their end just to look. The user is also setting up their own local venv (see README "Running locally") from the same `requirements.txt` so they can independently open and re-run notebooks/app in VS Code if they want to interact rather than just watch. Two separate venvs (cloud Linux + local Windows) are expected and fine — they're never meant to be the same folder/environment.
5. **Build cadence → bigger chunks, one full section at a time.** Build a coherent chunk of a notebook (e.g. all imports+setup, or the full default-data table, or the full calculation logic — a few cells), execute it, sync it, briefly describe what was added, then wait for the user's go-ahead before the next chunk. Not line-by-line/cell-by-cell pausing, and not the whole notebook in one shot either.
6. **Code commenting standard → every notebook, going forward.** Every new command/function call in a code cell gets a short trailing `# explains what this does` comment. Within a single cell, comment a given command/function only on its first occurrence — don't repeat the same explanation for repeated calls to it in that same cell (e.g. three `.to_csv()` calls in a row only need the comment on the first). It's fine, and expected, for the same command to be commented again if it reappears in a *different* cell (readers may open cells out of order). This was applied retroactively to all of `01_Connected_Load_Estimation.ipynb`.

## Still open (raised again at the relevant step, not blocking now)

1. ~~One public holiday date in the source is off by one day (labeled Jan 16, applied Jan 15)~~ — fixed in §2.3 and carried into the editable defaults (§2.9).
2. ~~No ROI/NPV/IRR formula exists in the source workbook — only LCOE.~~ — defined from scratch at Step 6 (simple/discounted payback, NPV, lifetime ROI), with the tariff assumption this requires flagged as an explicit placeholder — see Step 6 below.
3. **Known, accepted 4-day/year calendar simplification (§2.9).** The source workbook's real (non-buggy) behaviour switches weekend day-types from Early-Monsoon to Peak-Monsoon patterns starting July 18, a full 2 weeks before weekdays make the same switch (July 31). A single start/end date range per named season — as requested ("edit the season durations, starting and ending dates") — can't represent that weekday/weekend asymmetry. Decision: kept the simpler single-range-per-season model; 4 specific weekend days a year (Jul 18, 19, 25, 26) use the previous season's weekend pattern instead of the new one. Effect on annual demand: +0.089% (3.437285 GWh -> 3.440338 GWh) when switching from the as-implemented calendar to the editable one. Revisit only if this ever needs to be exact — would mean giving weekday and weekend independent date ranges per period.

## Environment

- Cloud sandbox project root: `/home/claude/rural-electrification/`
- Python venv: `venv/` (Linux, cloud-sandbox only — cannot run on the user's Windows machine; see `requirements.txt` header)
- Jupyter kernel registered as "Rural Electrification (venv)"
- **Cloud sandbox has no general internet access** — outbound network reaches package registries (pip/npm) only; calls to real-world APIs (e.g. PVGIS, NASA POWER — see Step 4) fail here with a connection error and cannot be executed/demonstrated live in this environment. Any notebook cell making a live external API call needs its own offline/mocked test to be verified (see Step 4 §4.4-4.5 for the pattern used), and should be re-run on the user's own machine or a deployed server to confirm the live call itself.
- Folder structure:
  - `notebooks/` — one notebook per core step (built incrementally, only when user says go)
  - `data/` — default assumption tables (appliance lists, misc loads, costs) as editable CSV/YAML, extracted from the reference workbook
  - `app/` — Streamlit app (`app/Home.py` + `app/pages/`)
  - `docs/` — this log + full workbook methodology reference

## Status

- [x] Workbook read and fully mapped (methodology doc written).
- [x] File renamed to `Base-Calculations-00.xlsx` per user request; original left in place (no delete access to user's device from this session).
- [x] Venv + requirements.txt created and installed (relaxed to minimum-version pins after a local Windows install hit a source-build error on exact pins — see git history / chat).
- [x] Project skeleton created.
- [x] **Step 1 — Connected Load Estimation (`notebooks/01_Connected_Load_Estimation.ipynb`) — DONE.**
  - Default data (household categories, per-category appliances, misc. loads) built and saved to `data/default_*.csv`, matching Annex-I exactly.
  - Added a user-editable "Others" placeholder appliance row (0 W default) per category (A/B/C), per user request, so a new appliance can be added later without changing table structure.
  - `compute_connected_load()` implemented; total verified against workbook: **1.81128 MW**.
  - Excel export/import round-trip (`export_appliance_template()` / `import_appliance_template()`) built and tested, including a validation-rejection test (invalid category) and a full round-trip-matches-original test. Template file: `data/Connected_Load_Input_Template.xlsx`.
  - Two charts built (connected load by category; top 10 individual load contributors) using a fixed category->color mapping (A=blue, B=orange, C=aqua, Misc=yellow) to be reused across all later charts in this project.
- [~] **Step 2 — Hourly Load Profile (`notebooks/02_Hourly_Load_Profile.ipynb`) — IN PROGRESS.**
  - §2.2: 19 day-type profiles (8,208 Qty-Active values) extracted directly from Annex-II via openpyxl (not hand-typed), saved to `data/default_daytype_profiles.csv`. Verified against a known spot-check.
  - §2.3: 365-day calendar extracted from Annex-III's actual array formulas (as-implemented, bugs included), then patched: 42 days (Aug 3-13, Sep 16-Oct 30) moved from the buggy "April" reuse to the correct Peak-Monsoon/Late-Summer profiles; 2 days fixed the Jan 15->16 holiday offset. Saved to `data/annual_calendar_2026.csv`. All changes verified via automated checks (no stray April usage outside April, exactly 44 days changed, no duplicate/missing days).
  - §2.4: Full 8,760-hour demand profile computed (`Qty Active x Power x Household count`, matching Annex-II's own formula). Cross-validated: hour 1 matches the workbook exactly (261,300 Wh); annual total is 3.437285 GWh vs. the workbook's (buggy) 3.472586 GWh — a -1.017% difference, confirmed to be because Peak-Monsoon/Late-Summer day types carry less daily demand than April's in this workbook's own assumptions (checked directly, not assumed). An independent hand-calculation cross-check on one of the fixed hours (Aug 3, hour 0) matches the pipeline exactly. Saved to `data/hourly_load_profile_2026.csv`.
  - Noted limitation: the new "Others" placeholder appliance (Step 1) has no hourly usage pattern yet (Annex-II never modeled it), so it currently affects connected/peak load only, not the hourly profile — flagged as a follow-up, not silently ignored.
  - §2.6: Excel download/edit/re-upload for the day-type table (pivoted wide for easy editing), round-trip tested exactly (`pd.testing.assert_frame_equal`).
  - §2.7: Alternative path — download a blank Date/Hour/Load_kWh template, fill in a full custom hourly profile, upload directly (bypasses day-type computation entirely). Round-trip tested, plus a negative test confirming incomplete uploads are rejected rather than silently treated as zero demand.
  - §2.9: Calendar rules made fully user-editable, per user request. Three small tables now drive the calendar instead of the as-implemented Annex-III formulas: `default_season_periods` (12 named periods, editable start/end MM-DD dates + weekday/weekend day-type per period), `default_public_holidays` and `default_festival_holidays` (each individual dates, not ranges). `build_calendar_from_rules()` generates the 365-day calendar from these; `validate_season_periods()` guards against a user edit leaving a gap or overlap. Cross-checked day-by-day against the proven §2.3 calendar: matches exactly except 4 documented weekend days — see "Still open" item 3 above. `editable_calendar` (and the hourly profile computed from it) is now the project's default going forward; `data/annual_calendar_2026.csv` and `data/hourly_load_profile_2026.csv` were re-saved from it. Excel download/edit/re-upload built and tested (`export_calendar_rules_template()` / `import_calendar_rules_template()`), including a negative test for a broken (gapped) edit.
  - **Step 2 is now DONE, including user-editable season/holiday/festival dates.** Files in `data/`: `default_daytype_profiles.csv`, `annual_calendar_2026.csv`, `hourly_load_profile_2026.csv`, `default_season_periods.csv`, `default_public_holidays.csv`, `default_festival_holidays.csv`, `Day_Type_Profiles_Template.xlsx`, `Hourly_Load_Profile_Template.xlsx`, `Calendar_Rules_Template.xlsx`.
- [x] **Step 3 — Load Profile Visualization (`notebooks/03_Load_Profile_Visualization.ipynb`) — DONE.**
  - Three chart functions built on top of Step 2's `hourly_load_profile_2026.csv`, each parameterized by display unit so the same code drives the eventual Streamlit unit dropdown: `plot_weekly_profile()` (stacked-area, 168-hour window, by category, `start_day` selectable), `plot_monthly_totals()` (stacked-bar, 12 months, by category, direct-labeled totals), `plot_yearly_overview()` (single-line daily-total trend, full year, annual total in the title).
  - `convert_wh()` centralizes kWh/MWh/TWh conversion; verified the annual total converts consistently across all three (3,440,337.926 kWh = 3,440.337926 MWh = 0.003440 TWh).
  - Confirmed the unit switch changes the actual plotted values (redrew the monthly chart in all three units) and that the weekly view works for an arbitrary week, not just week 1 (demoed a Peak-Monsoon week in August alongside the default first week of January).
  - Charts use matplotlib (static), consistent with Steps 1-2, using the same fixed `CATEGORY_COLORS` mapping as every other chart in this project. Flagged as likely to become interactive Plotly charts in the actual Streamlit app (already in `requirements.txt`), with the same color mapping and `convert_wh()` carrying over unchanged.
- [x] **Step 4 — Solar Resource & PV/Battery Sizing (`notebooks/04_Solar_Resource_and_PV_Battery_Sizing.ipynb`) — DONE** (live-API confirmation on a real network still outstanding, see below).
  - §4.1-4.10 (solar resource data sourcing) — DONE. Per user request, the app only ever needs lat/lon from the user:
    - **Default/offline dataset**: this project's own site's 8,760-hour irradiance extracted directly from the workbook (Annex-IV column G), saved to `data/default_irradiance_2026.csv`. Works with zero network access.
    - **Two built-in, no-signup live APIs**: `fetch_pvgis_hourly()` (default — PVGIS, EU JRC, the field-standard PV-design tool, no key, 30 req/s rate limit, covers Europe/Africa/Asia + Americas 60N-20S) and `fetch_nasa_power_hourly()` (fallback — NASA POWER, genuinely global coverage, no key). Both confirmed to need no signup/account — researched directly from each provider's official API docs (not assumed). Both parse a real historical year's calendar dates onto this project's generic 365-day calendar (`standardize_to_project_year()`, drops Feb 29, averages across years if more than one requested).
    - **Custom API slot**: `data/solar-api.txt` (plain `key = value` config, template auto-created, not overwritten if the user edits it) — for a provider that DOES require a signup + API key (e.g. Solcast, confirmed to require an account). Nothing in the code needs to change to add one, only that file. The generic pass-through call works for a similarly-shaped API; a genuinely different response format will need a small parser added once the user has signed up and a real response can be seen.
    - **Excel upload/download**: `export_irradiance_template()` / `import_irradiance_template()`, same validated round-trip pattern as every other dataset in this project. Template: `data/Irradiance_Template.xlsx`.
    - **`get_solar_resource(lat, lon, year, source)`**: the single accessor tying all four together, with a graceful, clearly-flagged fallback to the default dataset if a live source fails.
    - **Known sandbox limitation**: this cloud sandbox's outbound network only reaches package registries — it cannot actually reach PVGIS or NASA POWER, so the live fetch could not be demonstrated end-to-end here (confirmed via a real attempt, which failed with a connection error as expected and triggered the fallback correctly). To compensate, the response-parsing logic (timestamp extraction, calendar mapping, leap-day handling, cross-year averaging, error handling) was unit-tested against hand-built mock responses in each provider's real shape, plus a negative test for a simulated connection failure — all passed. **The live network call itself should be re-tested once this notebook runs on the user's own machine or a deployed server with normal internet access.**
  - §4.11-4.16 (Ppeak, Egen, battery sizing) — DONE.
    - **PV system parameters** made fully user-editable via `data/default_pv_parameters.csv` / `PV_Parameters_Template.xlsx` (Excel round-trip): `performance_ratio_Q` (0.85), `reference_irradiance_iqc_kwm2` (1.0), `ppeak_w` (2,000,000 — starting default, matches the workbook's own choice for this demand profile), `battery_dod` (0.80), `battery_quality_factor` (0.85, replacing the workbook's undocumented x0.90 per your 2026-09-02 decision). **Flagged clearly**: `performance_ratio_Q` and `battery_quality_factor` are two different parameters that happen to share the 0.85 default — kept deliberately separate, never merged.
    - `compute_egen()`, `compute_delta_e_and_blocks()`/`block_sums()`, `size_battery()`, `simulate_battery_soc()`, `count_zero_yield_hours()` built and validated to the exact Wh/kWh/hour against the workbook's own cached Annex-IV values (Egen annual total 3,839,823,405 Wh; worst/best deficit blocks -7,141,922 / 8,860,118 Wh; battery size 8,000 kWh; zero-yield hours 384) — using the workbook's own demand column and its 0.90 factor, purely as a validation checkpoint.
    - Switched to this project's own numbers (corrected Steps 2-3 demand + 0.85 battery quality factor): battery size unchanged at **8,000 kWh** (same MROUND bracket), zero-yield hours drops to **317** (from 384). Isolated which change caused it: entirely the calendar-fix demand reduction, not the quality-factor change (which alone leaves both numbers unchanged at this site's numbers) — quantified, not assumed.
    - Added a battery-size sensitivity sweep (`battery_size_sensitivity()`) — the interactive "try different battery sizes" view the workbook itself doesn't have (per the methodology doc's own §7.4 note) — charted as a diminishing-returns curve, becomes a live slider in the app.
    - Saved `data/pv_battery_simulation_2026.csv` (full per-hour demand/irradiance/generation/deficit/SOC table) and `data/pv_battery_sizing_results_2026.csv` (scalar results: annual generation, battery size, zero-yield hours) for Step 5 (LCOE denominator) and Step 6 (summary table) to consume.
  - **Step 4 is now DONE**, except: the live PVGIS/NASA POWER fetch (§4.9) still needs to be confirmed working end-to-end on the user's own machine or a deployed server — only the fallback path could be exercised in this sandbox (see §4.9's note and the "Environment" section above).
- [x] **Step 5 — Cost / BOQ / LCOE (`notebooks/05_Cost_BOQ_LCOE.ipynb`) — DONE.**
  - **BOQ**: 17-line itemized bill of quantities extracted from Annex-VI, saved to `data/default_boq_items.csv`. Validated to the exact PKR against the workbook (604,000,000 PKR total). **Flagged plainly**: only 3 of the 17 lines have nonzero cost in the source (Solar Panels, Battery System, Cables & Distribution) — the other 14 are genuinely zero, an unfinished BOQ in the source, not a design choice; every line stays user-editable. The Solar Panels and Battery System lines are now **live-computed** from Step 4's Ppeak and battery size (`compute_boq_total()`), implementing the methodology doc's own flagged fix rather than leaving them as static numbers.
  - **Land cost**: `default_land_cost_options.csv` (buy / private_lease / govt_lease). Flagged: the source workbook only ever used the govt-lease figure (via Opex) — buy/private-lease were reference-only and never fed into any total. Made explicit via an editable `land_cost_approach` parameter instead of leaving that silent.
  - **O&M present value**: `compute_om_present_value()` — year-1 cash flow (insurance % of capital + $/kW/yr O&M, converted to PKR) inflated 8%/yr then discounted 10%/yr over 30 years. Validated to the exact PKR against the workbook (85,257,391.43 PKR).
  - **LCOE**: `compute_total_opex()` + `compute_lcoe()`. Validated against the workbook's own (demand-based) figure first (10.7879 PKR/kWh, exact match using the literal workbook demand figure) — then switched to **generation-based** per your 2026-09-02 decision: **9.7562 PKR/kWh** (~9.6% lower, since generation exceeds demand by design).
  - **Currency toggle** (`convert_currency()`): PKR / EUR / USD via two independent, editable exchange rates.
  - **Lump-sum override**: `use_lump_sum_capex` bypasses the itemized BOQ entirely when set — demonstrated and confirmed working alongside the itemized path.
  - Full pipeline (`run_cost_lcoe_pipeline()`) ties BOQ -> optional lump-sum override -> O&M PV -> total Opex (incl. chosen land approach) -> generation-based LCOE.
  - Excel round-trip (`export_cost_template()` / `import_cost_template()`, 3 sheets: BOQ Items, Land Cost Options, Cost Parameters), round-trip tested exactly. Template: `data/Cost_BOQ_Template.xlsx`.
  - Files in `data/`: `default_boq_items.csv`, `default_land_cost_options.csv`, `default_cost_parameters.csv`, `Cost_BOQ_Template.xlsx`, `cost_lcoe_results_2026.csv`.
- [x] **Step 6 — Summary Table (`notebooks/06_Summary_Table.ipynb`) — DONE.**
  - **Master summary**: every prior step's headline figure (connected load, annual demand, PV/battery size, zero-yield hours, capex, opex, LCOE, ROI/NPV/payback, USD conversion) pulled into one table — `data/master_summary_2026.csv`.
  - **ROI and payback — built from scratch**, since the source workbook has no revenue model at all (cost/LCOE only). `electricity_tariff_pkr_per_kwh` (`data/default_roi_parameters.csv`) defaults to an explicit **break-even placeholder** (= the computed LCOE), clearly flagged as the one input to replace with a real planning assumption (planned tariff, subsidy, or avoided-cost figure) — not a researched market rate.
  - `compute_om_cash_flow()` + `simulate_project_cashflow()`: full year-by-year nominal and discounted cash flow (revenue = energy served x tariff x escalation; costs = O&M/insurance + one-time battery/inverter replacement in a configurable year + land), simple/discounted payback via linear interpolation of the crossing year, NPV = final year's cumulative discounted cash flow, lifetime ROI = (total nominal net cash flow - capex) / capex.
  - Ran two scenarios: the break-even placeholder (NPV -511.3M PKR, ROI -80.8%, never pays back) and an illustrative +50% tariff (NPV -353.1M PKR, ROI +2.6%, ~28.8-year simple payback, still no discounted payback within 30 years at a 10% discount rate).
  - **Diagnosed and quantified why "tariff = LCOE" isn't actually break-even** in the cash-flow model (not a bug): (1) this model's O&M is a full nominal 8%/yr-escalating sum (~456.3M PKR) vs. the discounted present value Step 5's LCOE actually charged for (~85.3M PKR) — a ~371.0M PKR gap; (2) revenue is earned only on energy *demanded*, while LCOE's denominator is energy *generated* — a further ~116.9M PKR shortfall. Together these explain the bulk of the negative NPV at the "break-even" tariff. Documented in-notebook (§6.2) with the actual computed numbers, not hand estimates.
  - Cumulative discounted cash-flow chart (both scenarios, 30 years, discounted-payback year marked when reached within the horizon) — same validated `CATEGORY_COLORS` palette as every other chart in this project.
  - Excel round-trip (`export_roi_template()` / `import_roi_template()`), same validated pattern as every other dataset. Template: `data/ROI_Parameters_Template.xlsx`.
  - **Project Summary Report** (`data/Project_Summary_Report.xlsx`) — the consolidated deliverable this step was named for: one workbook, four sheets (Key Results, both cash-flow scenarios, ROI Parameters).
  - Files in `data/`: `default_roi_parameters.csv`, `master_summary_2026.csv`, `ROI_Parameters_Template.xlsx`, `Project_Summary_Report.xlsx`, `roi_results_2026.csv`.

**All six planned notebooks are now complete and validated against the source workbook wherever the workbook defines the calculation.**
- [x] **Streamlit app (`app/Home.py` + `app/pages/` + `app/core/`) — DONE.** The runnable deliverable: open `app/Home.py` and every step becomes an interactive page.
  - `app/core/` — one module per step (`load_estimation.py`, `load_profile.py`, `solar_pv.py`, `cost_lcoe.py`, `summary_roi.py`), each a near-verbatim port of that notebook's validated functions (same formulas, same variable names where practical), plus `charts.py` (Plotly versions of every notebook chart, same `CATEGORY_COLORS` palette) and `paths.py`/`style.py`.
  - `app/pages/1_Connected_Load.py` … `6_Summary_ROI.py` — one page per step, each with: editable tables (`st.data_editor`), Excel template download/upload (reusing every `export_*_template()`/`import_*_template()` from the notebooks unchanged), live-recomputed results and interactive charts, and a "Save" button that writes back to `data/*.csv` so the next page (and the notebooks, and re-opening the app later) all see the same numbers. Step 4 exposes lat/lon + source selector (default/PVGIS/NASA POWER/custom) exactly as planned; Step 6 carries the same break-even-tariff placeholder and warning as the notebook, plus an adjustable comparison-tariff slider.
  - **Verified two ways**: (1) `streamlit.testing.v1.AppTest` run on all 7 pages (Home + 6 steps) with zero exceptions, including clicking every "Save" button through the full Step 1 → 2 → 4 → 5 → 6 chain and confirming the results match the notebooks exactly (battery 8,000 kWh, zero-yield hours 317, LCOE 9.7562 PKR/kWh, NPV -511,307,349 PKR at the break-even tariff — bit-for-bit the same as the notebooks); (2) a real `streamlit run` boot in this sandbox, confirmed serving HTTP 200.
  - Same sandbox limitation as Step 4's notebook: PVGIS/NASA POWER can't be reached from here, so the live-fetch buttons on the Solar PV page need confirming once run on a machine with normal internet access — the default/offline dataset and Excel upload paths both work regardless.
  - **This is the file to open and run**: `streamlit run app/Home.py` (from the project root, with the venv activated).

- [x] **App redesign, Phase 4 (post-first-test user feedback) — DONE.** After testing the first app build, the user asked for a redesign: an "app, not spreadsheet" UI, EUR as the base currency, new charts/formulas, and content additions. Implemented:
  - **Pages renamed** (sidebar label = filename, so files were renamed, not just re-titled): `1_Connected_Load.py`→**`1_Load_Setup.py`**, `2_Load_Profile.py`→**`2_Demand_Profile.py`**, `3_Visualization.py`→**`3_Energy_Insights.py`**, `4_Solar_PV_Battery.py`→**`4_Solar_Design.py`**, `5_Cost_BOQ_LCOE.py`→**`5_Financials.py`**, `6_Summary_ROI.py`→**`6_Results.py`**. **The 6 old-named files were left in place on disk** (this environment has no tool to delete/move files on the user's machine) — the user needs to manually delete the 6 old files from `app/pages/` or Streamlit's sidebar will show 12 pages instead of 6.
  - **Currency rebase, PKR → EUR**: every cost figure in `core/cost_lcoe.py` and `core/summary_roi.py` (and the underlying `data/default_cost_parameters.csv`, `default_boq_items.csv`, `default_land_cost_options.csv`, `default_roi_parameters.csv`) was converted from PKR to EUR **once, at the data level** (divided by the existing 330 PKR/EUR rate), with every `*_pkr` identifier renamed to `*_eur` throughout. EUR is now the app's base/default currency; PKR and USD remain available as display-only conversions (`convert_currency()`, now EUR-based) via a radio button on Financials and Results. Re-ran the actual EUR pipeline (not a manual estimate) to regenerate `cost_lcoe_results_2026.csv`, `default_roi_parameters.csv` (break-even tariff placeholder), `roi_results_2026.csv`, `master_summary_2026.csv` — confirmed bit-for-bit consistent with the original PKR figures at the 330 rate (e.g. capital cost 1,830,303.03 EUR × 330 = 604,000,000 PKR ✓). **Notebooks 5/6 were intentionally left in PKR** — only the live app was rebased; the notebooks remain the PKR validation record against the source workbook.
  - **New `data/default_site_info.csv` + `app/core/site_info.py`**: non-calculation site/context fields (project name, country, city/village, lat/lon, population, area, weather type, socio-economic class, GDP/capita, pre-project electrification %) for later socio-economic analysis and the Results page's project overview. Editable on Load Setup, Excel round-trip included. (Caught and fixed two unquoted-embedded-comma CSV bugs in this file and one in `default_cost_parameters.csv` before they could break `pd.read_csv` downstream — values with commas inside them must be quoted in CSV.)
  - **Household count from total houses + % split**: `load_estimation.apply_total_houses_split()` (largest-remainder rounding, so counts always sum exactly to the entered total) replaces the old raw editable `household_count` column — Load Setup now asks for "total number of houses" + each category's %, and computes counts live.
  - **New `app/core/ui.py`**: shared CSS theme (cards, stat tiles, disclaimer banner, nicer buttons/tabs) plus `page_header_image()` using six custom tab-icon illustrations (`app/assets/icons/*.png`, cropped from a reference sheet the user supplied) instead of plain titles — applied consistently across every page for the "app, not spreadsheet" look. Added the required **academic-project disclaimer** banner (Home + Results pages).
  - **Charting engine switched from Plotly to Apache ECharts** (`streamlit-echarts`, installed in the project's `venv`) per the user's explicit request — every chart builder in `core/charts.py` now returns an ECharts option dict rendered via `st_echarts()`. Categorical breakdowns (household split, connected-load-by-category, BOQ cost breakdown) became donut/pie charts with % labels; ROI is now also shown as a round gauge chart on Results.
  - **New charts**: `hourly_profile_chart()`/`daily_profile_chart()` (new), plus a granularity dropdown (Hourly/Daily/Weekly/Monthly/Annual) on Demand Profile and a matching tab set on Energy Insights, all sharing one line-chart visual style with explicit units/legends and peak/average/total stats shown underneath (`charts.load_stats()`). Solar Design gained `irradiance_vs_demand_chart()` (dedicated GHI-vs-demand, dual y-axis) and `duck_curve_chart()` (net load = demand − generation, single day or annual-average shape), plus the Egen/battery-sizing formulas shown explicitly via `st.latex()`.
  - **Solar Design subheading**: "Solar PV with Battery System", with an explicit note that a future version will support coupling with a diesel generator instead of/alongside the battery, per the user's stated plan.
  - **Results page redesign**: card/tile-based project overview (site info + house/population counts), system & cost summary tiles, ROI section with a gauge chart + cumulative cash-flow chart side by side — no more raw `st.dataframe`-only results.
  - **Verified**: `streamlit.testing.v1.AppTest` on all 7 pages (zero exceptions), every "Save" button clicked end-to-end, granularity/tab/radio/checkbox interactions exercised, plus a real `streamlit run` boot (HTTP 200, `/healthz` OK).
  - **Known gaps to flag to the user**: no image-generation tool is available in this environment, so illustrations beyond the six tab icons the user supplied were not created — the CSS card/tile theme is the substitute. `default_site_info.csv`'s population/GDP-per-capita/electrification-% are still placeholder estimates for the Shamspir site, not researched figures.

- [x] **Full rebuild: Streamlit → React + Tailwind + FastAPI — DONE.** The user explicitly rejected Streamlit's look ("not a Streamlit or data-science dashboard") and asked for a polished commercial SaaS/engineering application: React + Tailwind CSS, Apache ECharts (via `echarts-for-react`, not `streamlit-echarts`), Lucide React icons, a landing page of 6 illustrated step cards, and a persistent top stepper (checkmarks for completed steps, a dot for the current one). This supersedes the Streamlit `app/` — that folder is left on disk as a reference/fallback (this session can't delete files on the user's machine) but is no longer the active deliverable.
  - **Architecture**: `backend/` (FastAPI, Python) wraps the exact same `core/*.py` calculation modules (copied unchanged from `app/core/`, since they had zero Streamlit dependencies — verified before copying) behind a JSON REST API (`backend/api.py`, one router covering all 6 steps + a `/progress` and `/dashboard` endpoint for the landing page). `frontend/` (Vite + React 19 + Tailwind 3) is a separate SPA consuming that API over `fetch`. The two run as separate processes (backend :8000, frontend :5173 in dev) — see `README.md`/`Local-VSCode-Setup.md` for the two-terminal run instructions to add.
  - **Computation split, by design**: heavy/sequential work (8,760-hour calendar generation, the PV/battery SOC simulation, the cost/LCOE pipeline, the ROI cash-flow) stays server-side in Python, called via debounced POST requests as the user edits inputs (typically ~400ms debounce, imperceptible for these pure-pandas pipelines). Lightweight, purely-formulaic client feedback (the household total-houses/%-split live recompute, chart data slicing/aggregation for different granularities, unit and currency conversion, peak/average/total stats) was ported to parallel JS modules (`frontend/src/lib/{split,format,charts}.js`) so those specific interactions never round-trip to the server. This duplicates a handful of simple formulas across Python and JS on purpose — flagging here so a future change to e.g. `apply_total_houses_split()` in `backend/core/load_estimation.py` is remembered to also be mirrored in `frontend/src/lib/split.js`.
  - **All 6 steps + landing page built and verified end-to-end** against the live backend (headless-Chromium smoke tests of every route, zero console/runtime errors, a full `npm run build` production build, and a save-flow test on every page): Landing (hero + live KPI snapshot + 6 illustrated cards), Load Setup, Demand Profile (season/holiday/festival editable tables + a pivoted 24-hour x item-type grid for the 19 day-type usage profiles, replacing the old page's raw 8,208-row dataframe), Energy Insights (hourly/daily/weekly/monthly/annual views, all client-side against one cached fetch of the saved profile), Solar Design (irradiance fetch, PV parameters, plain-CSS formula blocks in place of `st.latex`, battery sensitivity / demand-vs-generation-vs-SOC / irradiance-vs-demand / duck-curve charts), Financials (BOQ/land/cost-parameter tables, EUR/PKR/USD currency toggle, BOQ cost-breakdown donut), Results (project overview, system & cost summary, ROI assumptions with the same break-even-placeholder warning, ROI gauge + cumulative cash-flow comparison chart, and a sectioned card-based master summary — explicitly not a raw table, per the user's requirement). Figures cross-checked against the previously-validated numbers throughout (connected load 1.81 MW, annual demand 3.4344 GWh, battery 8,000 kWh, zero-yield hours 312-317 depending on exact site-info state, capital cost 1,830,303.03 EUR, LCOE 0.0296 EUR/kWh) — no regressions from the Streamlit-era logic.
  - **Route-based code-splitting** (`React.lazy`/`Suspense` per step page in `App.jsx`) added after the first production build flagged a 1.45 MB main bundle — ECharts is now its own lazily-loaded chunk, and the landing page (which needs no charts) loads without it.
  - **Bug caught and fixed during testing**: an early Playwright test script mistakenly typed into the Load Setup page's latitude field (not "total houses" as intended) and saved it, corrupting `data/default_site_info.csv`'s latitude to 2000 — caught via the Results page showing "Lat/Lon: 2000, 66.919" and fixed by re-POSTing the correct value (24.843) to `/api/site-info`. Flagging in case the user notices this in git history/backups from around this session.
  - **Synced to the user's device** as `rural-electrification-webapp.zip` (backend/ + frontend/, excluding `node_modules/`/`venv/`/`__pycache__`/`dist/`) — this session has no shell on the user's machine, so a zip + manual extract was used instead of writing dozens of files individually. Root `README.md` and `requirements.txt` updated with the new two-process (backend + frontend) run instructions and added `fastapi`/`uvicorn`/`python-multipart` dependencies, and re-synced too.
  - **Still open**: whether to tell the user to delete the old `app/` Streamlit folder now that it's superseded — left in place for now since this session can't delete files on the user's machine; flag for the user to decide.
